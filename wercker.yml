box: wercker-labs/wercker
build:
  steps:
    - script:
        name: docker build
        code: |
          docker build -t misc .
    - script:
        name: docker run
        code: |
          docker run misc echo hello
